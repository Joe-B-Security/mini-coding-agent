"""Example hooks for mini-coding-agent Part 4.

Not a runtime dependency of the agent itself. The agent imports the hook
framework from the top-level `hooks.py` module. This directory holds the
example config (`example_hooks/hooks.json`) plus the shell scripts and
Python callables that config references.
"""
